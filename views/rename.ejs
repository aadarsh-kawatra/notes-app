<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Rename File</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen p-10 bg-zinc-900 text-white">
      <a
        href="/"
        class="inline-block px-4 py-1 bg-zinc-100 text-zinc-900 rounded-full text-sm"
      >
        &larr; Back to Home
      </a>
      <div class="mt-6">
        <h1 class="text-3xl text-center">Rename File</h1>
        <form
          action="/rename"
          method="post"
          class="mt-2 max-w-3xl mx-auto grid gap-4"
        >
          <input
            type="text"
            name="prev_name"
            placeholder="Previous Name"
            class="bg-zinc-700 px-4 py-2 rounded"
            value="<%= filename %>"
            readonly
          />
          <input
            type="text"
            name="new_name"
            placeholder="New Name"
            class="resize-none bg-zinc-700 px-4 py-2 rounded"
          />
          <input
            type="submit"
            value="Edit Name"
            class="px-4 py-2 w-fit mx-auto rounded bg-zinc-100 text-zinc-900 hover:bg-zinc-200"
          />
        </form>
      </div>
    </div>
  </body>
</html>
